@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@model Dossier
@{
    @*
        ################ TO DO ################
        #   Make dossier number dynamic!!     #
        ################ TO DO ################
    *@
    ViewData["Title"] = "Dossier #";
}

<body>
    <div class="container">
        <h1>Details @ViewData["Title"]</h1>
        @{
            Dossier dossier = ViewBag.Dossier;
            List<String> RoomList = new List<String>();
            foreach (Room r in ViewBag.Rooms)
            {
                RoomList.Add(r.RoomName);

            }
            List<Diner> DinerList = new List<Diner>();
            foreach (Diner d in ViewBag.Diner)
            {
                DinerList.Add(d);
            }
            List<int> DinerIdList = new List<int>();
            foreach(Diner d in DinerList)
            {
                DinerIdList.Add(d.DinerId);

            }

        }
        <form asp-controller="Dossier" asp-action="DossierUpdate" method="post" class="mt-3">
            <div class="form-group row">
                <label asp-for="DossierName" class="col-sm-2 col-form-label">Dossiernaam:</label>
                <div class="col-sm-10">
                    <input asp-for="DossierName" value="@dossier.DossierName" disabled class="form-control" />
                </div>
            </div>
            <div class="form-group row">
                <label asp-for="CustomerId" class="col-sm-2 col-form-label">Klant: </label>
                <div class="col-sm-10">
                    <input asp-for="CustomerId" value="@dossier.CustomerId" disabled class="form-control" />
                </div>
            </div>
            <div class="form-group row">
                <label asp-for="PartyType" class="col-sm-2 col-form-label">Soort feest:</label>
                <div class="col-sm-10">
                    <input asp-for="PartyType" value="@dossier.PartyType" disabled class="form-control" />
                </div>
            </div>
            <div class="form-group row">
                <label asp-for="DatePlanned" class="col-sm-2 col-form-label">Datum: </label>
                <div class="col-sm-10">
                    <input type="text" asp-for="DatePlanned" value="@dossier.DatePlanned.ToShortDateString()" disabled class="form-control" />
                </div>
            </div>
            <div class="form-group row">
                <label asp-for="Budget" class="col-sm-2 col-form-label">Budget: </label>
                <div class="col-sm-10">
                    <input asp-for="Budget" value="@dossier.Budget" disabled class="form-control" />
                </div>
            </div>
            @*<div class="form-group row">
            <label asp-for="DossierRoomList" class="col-sm-2 col-form-label">Zaal</label>
            <div class="col-sm-10">
                <select class="form-control" asp-for="DossierRoomList"
                        asp-items="@(new SelectList(RoomList))">
                    <option type="checkbox" value="" disabled selected>Selecteer zaal</option>
                </select>
                <span asp-validation-for="DossierRoomList" class="text-danger"></span>
            </div>
        </div>
        <div class="form-group row">
            <label asp-for="DossierDinersList" class="col-sm-2 col-form-label">Diner</label>
            <div class="col-sm-10">
                <select class="form-control" asp-for="DossierDinersList"
                        asp-items="@(new SelectList(DinerIdList))">
                    <option type="checkbox" value="" disabled selected>Selecteer diner</option>
                </select>
                <span asp-validation-for="DossierDinersList" class="text-danger"></span>
            </div>
            <div id="dinerDetails">

            </div>*@
            <div class="form-group row">
                <button type="submit" class="btn btn-primary">Voeg zaal toe</button>
            </div>
            <div class="form-group row">
                <button type="submit" class="btn btn-primary">Voeg diner toe</button>
            </div>
        </form>
    </div>
</body>